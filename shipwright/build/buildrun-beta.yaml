# buildrun-beta.yaml - Clean v1beta1 implementation
apiVersion: shipwright.io/v1beta1
kind: BuildRun
metadata:
  name: java-webapp-buildrun-beta
  namespace: {{NAMESPACE}}
spec:
  buildRef:
    name: java-webapp-build-beta
  # No paramValues needed - system parameters auto-injected
  # User parameters inherited from Build spec