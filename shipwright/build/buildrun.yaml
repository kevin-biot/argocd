# buildrun.yaml
apiVersion: shipwright.io/v1beta1
kind: BuildRun
metadata:
  name: java-webapp-buildrun
  namespace: {{NAMESPACE}}
spec:
  build:
    name: java-webapp-build
  # No explicit parameters - let Shipwright auto-inject after API version fix
